@model blackWood.Models.TaiKhoan

@{
    ViewBag.Title = "XoaND";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutPageAdmin.cshtml";
}
<style>
    .table1 {
    }

    .table1, tr, td, th {
        border: 1px solid #333;
        border-collapse: collapse;
    }

        .table1 td, .table1 th {
            padding: 10px;
        }
</style>


<div id="content" class="bg-container">
    <header class="head">
        <div class="main-bar">
            <div class="row no-gutters">
                <div class="col-lg-6">
                    <h4 class="nav_top_align skin_txt">
                        <i class="fa-solid fa-user"></i>
                        Xoá thông tin người dùng
                    </h4>
                </div>
                <div class="col-lg-6">
                    <ol class="breadcrumb float-right nav_breadcrumb_top_align">
                        <li class="breadcrumb-item">
                            <a href="index1.html">
                                <i class="fa fa-home" data-pack="default" data-tags=""></i>
                                Trang chủ
                            </a>
                        </li>
                        <li class="breadcrumb-item">
                            <a href="#">Người dùng</a>
                        </li>
                        <li class="breadcrumb-item active">Xoá người dùng</li>
                    </ol>
                </div>
            </div>
        </div>
    </header>

    <div class="outer">
        <div class="inner bg-container">
            <div class="card">
                <div class="card-body m-t-35">
                    <h2 class="text-center mb-5">Bạn có chắc chắn muốn xoá tài khoản này không?</h2>
                    <table class="w-100 m-auto table1 ">
                        <tr>
                            <th class="text-center">Tên người dùng</th>
                            <td>@Html.DisplayFor(model => model.TenNguoiDung)</td>
                        </tr>

                        <tr>
                            <th class="text-center"> Tên tài khoản</th>
                            <td>@Html.DisplayFor(model => model.TaiKhoan1)</td>
                        </tr>

                        <tr>
                            <th class="text-center">Quyền</th>
                            <td>@Html.DisplayFor(model => model.Quyen)</td>
                        </tr>

                        <tr>
                            <th class="text-center">Email</th>
                            <td> @Html.DisplayFor(model => model.Email)</td>
                        </tr>
                    </table>

                    @using (Html.BeginForm())
                    {
                        @Html.AntiForgeryToken()

                        <div class="form-actions no-color mt-5">
                            <input type="submit" value="Xoá" class="btn btn-danger" />
                            @Html.ActionLink("Quay lại", "Index", null, new { @class = "btn btn-dark" })
                        </div>
                    }
                </div>

            </div>
        </div>

    </div>


</div>
